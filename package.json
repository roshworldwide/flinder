{
  "name": "flinder",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css script.js dist/ && sed -i 's/REPLACE_WITH_GROQ_API_KEY/'\"$GROQ_API_KEY\"'/g' dist/script.js"
  }
}
